Program = statement* ;

statement = exprStmt
          | funStmt
          | printStmt
          | ifStmt
          | whileStmt
          | forStmt
          | block ;

block = "{" declaration* "}" ;

declaration = varDecl
            | statement ;
