Program = statement* ;

statement = exprStmt
          | printStmt
          | ifStmt
          | whileStmt
          | forStmt
          | block ;

block = "{" declaration* "}" ;

declaration = varDecl
            | statement ;
